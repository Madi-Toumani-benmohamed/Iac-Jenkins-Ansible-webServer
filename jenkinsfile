 node{
      stage('Clone') {
          checkout scm
      }
      stage('Ansible') {
          ansiblePlaybook (
            inventory: 'hosts.yal',
            playbook: 'playbook.yml',
            colorized: true,
        )
      }
}
